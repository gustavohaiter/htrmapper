[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "htrmapper"
version = "0.1.0"
description = "Aerial photogrammetry desktop software for precision agriculture, with correct PPK/RTK-weighted bundle adjustment."
requires-python = ">=3.11"
license = { text = "Proprietary" }
dependencies = [
    "pillow>=10.0",
    "piexif>=1.1.3",
    "pyproj>=3.6",
    "numpy>=1.26",
    "psutil>=5.9",
    "matplotlib>=3.8",
    "pycolmap>=4.0,<5",
    "laspy>=2.5",
    "rasterio>=1.3",
    "scipy>=1.11",
]

[project.optional-dependencies]
gui = [
    "PySide6>=6.6",
]
dev = [
    "pytest>=8.0",
]

[project.scripts]
htrmapper = "htrmapper.cli.main:main"
htrmapper-gui = "htrmapper.gui.main_window:run"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
